version: '3.8'

services:
    python:
        build: 
            context: .
            dockerfile: docker/bbeauty/Dockerfile
        volumes: 
            - ./src:/app
        ports: 
            - 8000:8000
        command: "python manage.py runserver 0.0.0.0:8000"
        